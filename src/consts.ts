/**
 * Root for all the API calls to be made to the backend service
 *
 * To avoid CORS issues, this should be on the same domain as the single page application
 */
export const API_BASE_URL = '/api';
